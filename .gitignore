# Fish shell variables (machine-specific)
fish_variables

# SSH private keys (never commit these!)
id_*
*.pem
*.key

# Known hosts (machine-specific)
known_hosts
known_hosts.old

# KRDP credentials and TLS keys live in ~/.local/share/krdpserver/
# and are never committed
credentials
